# Stage 17 — Global Layout Repositioning

## Implemented updates

- Removed the old sidebar workspace description/search block from the left navigation across all roles.
- Moved each page's hero/header content into the global top app header.
- Removed the large in-page PageHeader card so the first content section now moves upward into its former position.
- Reduced metric/stat card height globally using compact MetricCard defaults.
- Tightened common dashboard/stat grid spacing.
- Added QA guard: `npm run lint:stage17`.

## QA

Passed:

```bash
npm run qa
```
